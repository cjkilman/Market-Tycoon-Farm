={"Reprocessed Value";
  ARRAYFORMULA(IF($B4:$B="","",
  LET(
  ids, B4:B,
  reproData, NR_REPRO_VALUE_TABLE,
  reproIDs, INDEX(reproData,,1),
  reproValues, INDEX(reproData,,3),
  MAP(ids, LAMBDA(id, 
    IF(id="",,
      XLOOKUP(id, reproIDs, reproValues, 0)
    )
  ))
)}